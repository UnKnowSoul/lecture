#include <stdio.h>
int main(){

    // การรับ input โดยใช้ gets และ fgets

    // char str[10];
    // gets(str);
    // fgets(str, 10, stdin);

    // printf("input - %s", str);

    // ----------------------------------

    // การรับ input โดยใช้ Array
    // use in 2,3

    // char str_i[10];
    // char str_f[10];
    // char str_l[10];

    // ---------------------------------- 2

    // การรับ string

    // char c = getchar();
    // printf("- %c", c);

    // ---------------------------------- 3

    // การรับตัวเลข

    // printf("Enter i :");
    // fgets(str_i, 10, stdin);
    // printf("Enter f :");
    // fgets(str_f, 101 stdin);
    // printf("Enter l :");
    // fgets(str_l, 10, stdin);

    // int i = atoi(str_i);
    // float f = atof(str_f);
    // long l = atol(str_l);

    // printf("%d - %f - %ld", i, f, l);

    // ----------------------------------

    //terminate chatacter, null character, ascii code 0

    //false
    // int found = 0;
    // float f2 = 0.0;
    // char c = '\0';

    
}